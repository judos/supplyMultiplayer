test use cases:

- level ends with enough items
- player presses next level button (with/without items)

Optional:
- Constant decider with signal for missing / requested items
- Health instead of instant failure of mission